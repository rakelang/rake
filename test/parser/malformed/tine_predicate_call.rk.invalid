rake malformed_tine_call(values: f32s) -> f32s:
  tine #active when sqrt(values) >= <0.0>

  through #active else <0.0> into active_value:
    values

  return sweep:
    | #active => active_value
    | _       => values
