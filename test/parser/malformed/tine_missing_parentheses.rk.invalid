rake malformed_tine(values: f32s) -> f32s:
  tine #active values >= <0.0>

  through #active else <0.0> into active_value:
    values

  return sweep:
    | #active => active_value
    | _       => values
